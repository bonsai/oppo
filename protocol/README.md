# OPPO Protocol Namespace

This directory defines the experimental URI/protocol namespace used by `oppo`.

- `oppp://` — URI scheme for identifying resources in the OPPO / Dial P2 world.
- Current status: project-private draft.
- Specification: [`oppp-uri.md`](./oppp-uri.md)

Example:

```text
oppp://call/p2
```
